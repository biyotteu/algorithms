#include <bits/stdc++.h>

using namespace std;

int main(){
	ios::sync_with_stdio(false);
	cin.tie(0);
	wcin.imbue( locale( "korean" ) );
	wcout.imbue( locale( "korean" ) );
	std::wcout << L("한글") << std::endl;
	wstring str;
	wcin >> str;
	wcout << str;
}