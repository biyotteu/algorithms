#include <bits/stdc++.h>

using namespace std;

vector<char> sk;
int main(){
	while(1){
		char x;
		scanf("%c",&x);
		if(x== '\n') break;
		if(('a'<=x && x<='z') || ('A'<=x && x<='Z')) sk.push_back(x);
	}
	int m;
	scnaf("%d",&m);
	while(m--){
		
	}
}